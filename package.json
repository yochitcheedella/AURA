{
  "name": "aura-reminder-assistant",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pages:dev": "wrangler pages dev dist --port 8787",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=aura-assistant"
  },
  "dependencies": {
    "@clerk/clerk-js": "^5.0.0",
    "@neondatabase/serverless": "^0.10.0",
    "jose": "^5.0.0",
    "svix": "^1.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
